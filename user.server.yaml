apiVersion: v1
kind: Service
metadata:
  name: message-web-server
spec:
  selector:
    app: message-web
  ports:
    - name: http-server-message
      protocol: TCP
      port: 9704
      targetPort: 9501
  type: NodePort